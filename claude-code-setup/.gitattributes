# PowerShell on Windows expects CRLF; the BOM in preflight.ps1 must survive
# checkout so Windows PowerShell 5.1 reads its Cyrillic strings as UTF-8.
*.ps1 text eol=crlf
*.cmd text eol=crlf
*.md  text eol=lf
